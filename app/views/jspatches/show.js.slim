= "// Title:   " + @jspatch.title + "\n"
= "// Apply:   " + @jspatch.app.identifier + "\n"
= "// Created: " + @jspatch.created_at.to_s + "\n"
= "// Updated: " + @jspatch.updated_at.to_s + "\n"

= raw @jspatch.script